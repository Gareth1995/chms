- Add member functionality (done)
    - Add backend code to calculate age given DOB (done)
- Add view member functionality (done)
    - Use ActionBar from ChakraUI for member table for CRUD actions (https://chakra-ui.com/docs/components/table#action-bar) (done)
    - Hex dob and email to create member_id (done)
    - Implement update functionality

======================================================
Feature: Update members
======================================================
Update functionality steps:
- When adding member add to both Members and Updates Tables. (done)
- When doing an update on the Members view:
    
    - First pull in row details of the member from the Members table 
    (we will use the member_id as target for updating and add it to update table upload for consistent member_id across all tables)
    
    - Do an update on the Members table.
    
    - Do an add member to the Updates table. (done)
        - Add update reason field on update members page

- Implement update funtion in backend
    - Add member updates (done)
    - Update member table (done)

======================================================
Feature: Track attendance
======================================================
From "track attendance" settings menu:
-> Page that asks for event specifics
    - Drop down of events (empty initially but will pull event names from Events table) (done)
    - Have a "+ Add event" as one of the drop down options  (done)
    - Also have - sign next to each event so that it allows for deletion (only on front end edit on_frontend column) (done)
    - Allow text to be typed and when enter is pressed it will save that event to an Events table on the backend. (done)
    - Also ask for event date (done)

-> Page like Members page where you can select rows and submit attendance.
    - Heading of Card will say "Track attendance for {event name}"
    - Selecting multiple individuals (from Member table) and submitting attendance
      will work even for dupes but backend just won't save dupes. This way you can also add attendance after the event
      by selecting track attendance in settings -> select the correct event name and date -> tick missed row -> submit again

Output to the Attendance table will be:
member_id | event_name | status | timestamp

Event table will have the following:
event_id | event_name |
Where event id will be in the form e{date}{5 digit random salt} eg e2026011109874

--------------------------------------------------------------------------------------
02/02/2026
--------------------------------------------------------------------------------------
- Change monthly and yearly counts to distinct members (done)

--------------------------------------------------------------------------------------
02/02/2026
--------------------------------------------------------------------------------------
- Add Church database (with church_id | church_name | church_address)
    - church_id will be assigned by admin as we add churches who opt in to the application (Done)

- Link every user to a church (when registering there should be a church selection dropdown) (done)

- When a user logs in that church_id is propogated throughout the front-end. (done)

- When we add members we add members with a church_id that matches the church_id from the user. (done)

- When we get members to display we filter on where church_id is equal to the church_id of the user.
    - View members component must be filtered on church_id (done)
    - Update members must take into account church_id (and add church change to UI) (done) church ID won't change we will just change member status.
        When a member goes to a different church also on the system we will be able to see them being added there
    - Members shown in track attendance must be filtered on church_id (done)
    - Data used to build line chart must be filtered on church_id 
    - gender distribution plot must be filtered on church_id
    - nationality distribution plot must be filtered on church_id
    - age distribution plot must be filtered on church_id



--------------------------------------------------------------------------------------
ToDo 02/26/2026
--------------------------------------------------------------------------------------
- Make member id a hex of first name + surname + random 4 digit salt
    - Make it so that only first name and surname is required
- By event selection,  change the size of the event dropdown to match the length of text it is holding.

--------------------------------------------------------------------------------------
NEXT STEPS
--------------------------------------------------------------------------------------

- Migrate this to a proper backend (do research and form best approach and using best solution)
- Plan out QR code attendance mechanism

